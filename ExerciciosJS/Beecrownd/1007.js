/*Leia quatro valores inteiros A, B, C e D. 
Calcule a diferença de A e B pelo produto de C e D.
Fórmula: DIFERENCA = (A * B - C * D)*/

let A = 0;
let B = 0;
let C = 0;
let D = 0;

let diferenca = A * B - C * D;

console.log(`DIFERENÇA = , ${diferenca}`);